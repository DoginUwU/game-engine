export default class Map {
    name = ""
    width = 0
    height = 0

    constructor(name: string, width: number, height: number) {
        this.name = name
        this.width = width
        this.height = height
    }
}